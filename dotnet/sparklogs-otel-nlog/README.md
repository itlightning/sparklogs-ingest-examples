# SparkLogs .NET + NLog (via MEL) + OpenTelemetry

Runnable project for [.NET ingestion](https://sparklogs.com/docs/ingest/data-sources/languages/dotnet) (NLog bridged through `Microsoft.Extensions.Logging` + OTel). See also the [OpenTelemetry SDKs guide](https://sparklogs.com/docs/ingest/data-sources/languages/opentelemetry-sdks).

```bash
make mock-test
```
